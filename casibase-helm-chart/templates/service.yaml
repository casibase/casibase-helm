apiVersion: v1
kind: Service
metadata:
  name: {{ include "casibase-helm-chart.fullname" . }}
  labels:
    {{- include "casibase-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "casibase-helm-chart.selectorLabels" . | nindent 4 }}
