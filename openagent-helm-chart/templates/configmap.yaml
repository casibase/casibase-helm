{{- if not .Values.appConfigFromSecret }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openagent-helm-chart.fullname" . }}-config
  labels:
    {{- include "openagent-helm-chart.labels" . | nindent 4 }}
data:
  app.conf: |
    {{- tpl .Values.appConfig . | nindent 4 }}
{{- end }}